{
  "version": "1.0.0",
  "private": false,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "docs:dev": "yarn workspace docs dev",
    "docs:build": "yarn workspace docs build",
    "docs:start": "yarn workspace docs start",
    "core:build": "yarn workspace @master-ui/core build"
  },
  "workspaces": [
    "packages/*",
    "docs"
  ]
}