{
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "yarn": "^1.22.17"
  },
  "scripts": {
    "docs": "yarn workspace docs",
    "docs:dev": "yarn workspace docs dev",
    "docs:build": "yarn workspace docs build",
    "docs:start": "yarn workspace docs start",
    "core": "yarn workspace core",
    "core:build": "yarn workspace core build",
    "react": "yarn workspace react",
    "react:build": "yarn workspace react build",
    "icon": "yarn workspace icon",
    "icon:build": "yarn workspace icon build",
    "bump-version": "lerna version --no-git-tag-version --no-private",
    "release": "yarn core:build && yarn react:build && lerna publish from-package --dist-tag latest",
    "build": "yarn core:build && yarn icon:build && yarn react:build",
    "postinstall": "yarn build"
  },
  "workspaces": [
    "packages/*",
    "docs"
  ]
}